<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.button :submit, 'Guardar', class: 'btn btn-primary' %>
  <%= link_to 'Cerrar', restaurants_path, class: 'btn btn-secondary' %>
<% end %>